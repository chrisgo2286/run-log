import { 
    validateUsername, 
    validatePassword1, 
    validatePassword2 
} from "./validationRules"

export function validateRegistration ({ username, password1, password2 }) {
    errors = []
    if (errors)


}